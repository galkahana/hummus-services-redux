name: Docker Image CI

on:
  push:
    tags:
      - "*"

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build --label hummus --tag hummus:latest --tag hummus:${{github.ref_name}} .
